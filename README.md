# local-ocr
Convert local images and PDF files to text data using OCR while preserving the document structure. By packaging as an .exe file, it can be used even in environments without the required runtime. Compatible with Windows. Uses Docling.
